name: Deploy to Apify Actor

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - '**/*.js'
      - 'package.json'
      - 'package-lock.json'
      - 'apify.json'
      - 'Dockerfile'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --legacy-peer-deps
      - name: Apify login
        env:
          APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
        run: npx apify@^3 login --token "$APIFY_TOKEN"
      - name: Push actor from apify.json
        run: npx apify@^3 push --no-prompt --purge
